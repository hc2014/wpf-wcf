<UserControl x:Class="AdminManager.UserControls.Authority_Plate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Loaded="UserControl_Loaded_1">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"></RowDefinition>
        </Grid.RowDefinitions>
        <UniformGrid Grid.Column="0" Grid.Row="0"  Name="UniformGrid1">

        </UniformGrid>
        <StackPanel Grid.Column="0" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="sure" Width="60" Height="30" Margin="5" Click="sure_Click_1">确定</Button>
            <Button Name="cancle" Width="60" Height="30" Margin="5" Click="cancle_Click_1">取消</Button>
        </StackPanel>
    </Grid>
</UserControl>
